{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Everything the Shelf window needs: core APIs (events, window) and deep links.",
  "windows": ["main"],
  "permissions": ["core:default", "deep-link:default"]
}
